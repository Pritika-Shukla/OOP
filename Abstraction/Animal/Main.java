package Abstraction.Animal;

public class Main {
    public static void main(String[] args) {
     Cat cat=new Cat();
     cat.sound();
     cat.name();
     Dog dog=new Dog();
     dog.sound();
     dog.name();
    }
}
